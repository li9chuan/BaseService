RootConfigFilename = "common.cfg";

AESAliasName = "lgc_0";
SId = 1;

DisplayedVariables += { "", "@LoadLua|loadlua", "@LoadConfig|loadconfig" };

LuaScript =
{
	"grow.lua",
	"calc.lua",
	"login.lua",
	"event_cb_base.lua",
};

Paths =
{
	"./script/",								// for lua root script
	"./script.pls/",							// for lua ext script
	"./static_table/",              		    // for static data
};

MaxPlayer                  =  50000;

PlayerLogoutTime           =   60*60*1000;		//	客户端连接的超时时间，超过将清除这个客户端的数据。
PlayerLogoutTimeCheck      =   5*60*1000;		//	客户端超时检查时间间隔。

