RootConfigFilename = "common.cfg";

AESAliasName = "fes_0";
SId = 66;

NegFiltersDebug = { "NET" };

GraphVars +=
{
	"FPSProcessMsg", "60000",	// every minute
};

StartLuaScript = "_FESMain.lua";

Paths =
{
	"./script/_FES/",							// for lua root script
	"./static_table/",              		    // for static data
};

DisplayedVariables += { "", "@LoadLua|loadlua", "@LoadMsg|loadmsg" };


MsgCount = true;
SaveEvent = false;

